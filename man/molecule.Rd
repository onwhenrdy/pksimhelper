% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create.R
\name{molecule}
\alias{molecule}
\title{Create a molecule object}
\usage{
molecule(
  name,
  display.name = NA,
  id = NA,
  file.name.match = NA,
  add.file.matcher = c(),
  pubchem.id = NA,
  MW = NA,
  is.fraction = FALSE,
  fixed.unit = NA,
  ylab = "Plasma Concentration",
  color = "black",
  pch = 19,
  lty = 1,
  in.legend = TRUE
)
}
\arguments{
\item{name}{Character. Name of the molecule (mandatory, non-empty).}

\item{display.name}{Character. Display name of the molecule (defaults to `name` if `NA` or empty).}

\item{id}{Character. Identifier of the molecule (defaults to `name` if `NA` or empty).}

\item{file.name.match}{Character. Pattern used to match files (defaults to `name` if `NA` or empty).
This is used to identify the molecule file columns.}

\item{add.file.matcher}{Character vector. Additional file matching patterns.
This is used to identify the molecule file columns.}

\item{pubchem.id}{Numeric or character. PubChem compound ID (optional).}

\item{MW}{Numeric. Molecular weight (optional; fetched from PubChem if `NA`).}

\item{is.fraction}{Logical. Whether the molecule is expressed as a fraction (default `FALSE`).
E.g. for fraction extrected in urin.}

\item{fixed.unit}{`units` object. Fixed unit of measurement (optional; must be of class `units` if provided).}

\item{ylab}{Character. Label for y-axis (default `"Plasma Concentration"`).}

\item{color}{Character. Colour for plotting (default `"black"`).}

\item{pch}{Integer. Point character for plotting (default `19`).}

\item{lty}{Integer. Line type for plotting (default `1`).}

\item{in.legend}{Logical. Whether to include this molecule in plot legends (default `TRUE`).}
}
\value{
An object of class `molecule`, which is a list with elements:
\itemize{
\item name
\item display.name
\item id
\item file.name.match
\item add.file.matcher
\item pubchem.id
\item MW
\item fixed.unit
\item is.fraction
\item color
\item ylab
\item pch
\item lty
\item in.legend
}
}
\description{
Constructs a `molecule` object containing metadata about a chemical compound,
optionally fetching the molecular weight and PubChem ID if not provided.
}
\details{
If `MW` is not provided, the function queries the PubChem REST API using either
`pubchem.id` or `name` to retrieve the molecular weight and PubChem ID.
If multiple PubChem entries are found, the function stops with an error.
}
\examples{
# Minimal example
\dontrun{
mol <- molecule("Caffeine")

# With fixed MW and display name
mol <- molecule("Caffeine", display.name = "Caffeine (custom)", MW = 194.19)
}
}
